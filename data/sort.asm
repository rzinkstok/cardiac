# Quicksort implementation
# 
# Put (input) in array SOURCE
# Put len(input) in N
# Create array RESULT
# Create array LOWER
# Create array HIGHER
#
# ROUTINE:
#   PIVOT <- SOURCE[N/2]
#   Put other elements in array CURRENT
#   Loop over CURRENT
#       if C <= PIVOT: put in LOWER
#       if C > PIVOT: put in HIGHER
#   Put PIVOT in RESULT at correct index
#   If LOWER is not empty:
#       (input) = LOWER
#       ROUTINE
#   if HIGHER is not empty:
#       (input) = HIGHER
#       ROUTINE
    
